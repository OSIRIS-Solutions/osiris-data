[project]
name = "osirisdata"
version = "0.1.1"
readme = "README.md"
requires-python = ">= 3.12"
dependencies = [
  "pymongo",
  "nameparser",
  "levenshtein",
  "requests",
  "pydantic",
  "pydantic-extra-types",
  "pyyaml"
]
license = "MIT"
